export type registerProps = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    number: string;
    dob: string;
    otp: string;
}

export type loginProps = {
    email: string,
    password: string,
    otp: string;
}